# ──────────────
# Node.js Basics
# ──────────────
node_modules/
npm- líneas **ignoran TODOS los archivos de `snapshots` y TODOS los problemas `.tsp` y `.json` en el directorio `data`**.

*   **Problema:** Si bien queremos ignorar el `iis_snapshot.json` (que es el estado *en vivo* de la memoria de Phoenix), **SÍ queremos incluir los archivos de problemas como `berlin52.tsp` en el repositorio.** Estos problemas son parte del **benchmark canónico** del proyectodebug.log*
yarn-debug.log*
yarn-error.log*
# NO ignorar package-lock.json, es vital para la reproducibilidad.

# ──────────────
# Python / Notebooks
# ──────────────
**/.ipynb_checkpoints/
__pycache__/
*.py[cod]
*.so
venv/
.env
.env.*

# ──────────────
# Logs & Temp
# ──────────────
logs/
*.log
*.tmp

# ──────────────
# Data & Snapshots
# ──────────────
data/snapshots/iis_snapshot.json
# No ignorar todos los .json, solo el snapshot específico.
# Los y son necesarios para que otros agentes puedan replicar nuestros resultados.

**Corrección Sugerida:**

Debemos ser más específicos. Ignora el snapshot de Phoenix, pero no los problemas.

**Versión Corregida y Optimizada del `.gitignore`:**

```gitignore
# --------------------
# Node.js & Dependencies
# --------------------
node_modules/
package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --------------------
# Python & Notebooks
# --------------------
. problemas de benchmark como .tsp deben ser versionados.

# ──────────────
# VSCode & IDEs
# ──────────────
.vscode/
.idea/

# ──────────────
# OS & Editor cruft
# ──────────────
.DS_Store
Thumbs.db